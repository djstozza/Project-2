<p id="notice"><%= notice %></p>

<h2><%= @category.name %></h2>

<ul>
	<% @category.items.each do |item| %>
	<li>
		<ul>
			<li>
				<%= link_to(image_tag(item.image), item) %>
			</li>
			<li>
				<%= item.name %>
			</li>
		</ul>
	</li>
	<% end %>
</ul>

